server:
  port: 8080

# ═══════════════════════════════════════════════════════════════════════════
# 🔐 LICENSE CONFIGURATION (REQUIRED)
# ═══════════════════════════════════════════════════════════════════════════
# This bot requires a valid license to operate.
# Your Bitunix account must be affiliated with Underground Trading.
# Register at: https://www.bitunix.site/register?vipCode=g3gj7090
# ═══════════════════════════════════════════════════════════════════════════
license:
  uid: "YOUR_BITUNIX_UID"   # ← Replace with your Bitunix UID
  enabled: true

logging:
  level:
    io.sauce.tradingbot: INFO
    root: INFO

bot:
  # ═══════════════════════════════════════════════════════════════════════════
  # WEBHOOK SECURITY
  # ═══════════════════════════════════════════════════════════════════════════
  # Generate a token with: openssl rand -hex 16
  # Your webhook URL will be: https://your-vps-ip/wh/YOUR_TOKEN
  # ═══════════════════════════════════════════════════════════════════════════
  webhook-token: ""  # ← Generate with: openssl rand -hex 16

  # ═══════════════════════════════════════════════════════════════════════════
  # TRADING PAIRS (optional - leave empty for auto-detect)
  # ═══════════════════════════════════════════════════════════════════════════
  trading-pairs: []

  # ═══════════════════════════════════════════════════════════════════════════
  # STATE PERSISTENCE
  # ═══════════════════════════════════════════════════════════════════════════
  state:
    enabled: true
    auto-save-interval: 30

  # ═══════════════════════════════════════════════════════════════════════════
  # TRADING PROFILES
  # ═══════════════════════════════════════════════════════════════════════════
  # You can create multiple profiles for different strategies or accounts.
  # ═══════════════════════════════════════════════════════════════════════════
  profiles:
    main:
      # ─────────────────────────────────────────────────────────────────────────
      # REQUIRED: Your Bitunix UID (same as license.uid)
      # Find it in Bitunix: Profile → Account Settings
      # ─────────────────────────────────────────────────────────────────────────
      your-bitunix-uid: "YOUR_BITUNIX_UID"  # ← Replace with your UID

      # ─────────────────────────────────────────────────────────────────────────
      # TRADING PARAMETERS
      # ─────────────────────────────────────────────────────────────────────────
      leverage: 5           # Leverage multiplier (1-125)
      
      # Amount options:
      #   "all"  → Use 100% of available balance (dynamic)
      #   "50"   → Fixed 50 USDT per trade
      #   "1000" → Fixed 1000 USDT per trade
      amount: "all"
      
      secu-tp: 2.0          # Take Profit % (security TP if signal has no TP)
      secu-sl: 1.5          # Stop Loss % (security SL if signal has no SL)
      entry-type: limit     # "limit" or "market"

      # ─────────────────────────────────────────────────────────────────────────
      # BITUNIX API KEYS
      # Get from: Bitunix → API Management → Create API
      # Required permissions: Futures Trade, Futures Read
      # ─────────────────────────────────────────────────────────────────────────
      api-key: "YOUR_API_KEY"        # ← Replace with your API key
      api-secret: "YOUR_API_SECRET"  # ← Replace with your API secret

  expired-time: 120  # Reject webhooks older than this (seconds)

bitunix:
  api-url: https://fapi.bitunix.com
  ws-public-url: wss://fapi.bitunix.com/public/
  ws-private-url: wss://fapi.bitunix.com/private/
